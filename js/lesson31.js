const showMessage = function() {
    return "Olá meus bora bill";
}

const logMessage = function(message) {
    return "Olá, " + message;
}

const response = logMessage("Bora bill")

console.log(showMessage);
console.log(logMessage("Bora Bill"));
console.log(response);
